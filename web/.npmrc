; npm ci on CI is strict about peer deps and fails on the unocss@0.61 <-> vite@6
; range skew (unocss declares peer support only up to Vite 5). UnoCSS runs fine
; on Vite 6; this makes CI resolve the same way local `npm install` already does.
legacy-peer-deps=true